<?xml version="1.0" encoding="utf-8"?>
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- لایه‌ی بیرونی (بردر کلفت) -->
    <item>
        <shape android:shape="rectangle">
            <solid android:color="@color/transparent" /> <!-- رنگ پس‌زمینه -->
            <corners android:radius="36dp" />
            <stroke
                android:width="1dp"
                android:color="@color/littleStroke" /> <!-- بردر کلفت -->
        </shape>
    </item>

    <!-- لایه‌ی داخلی (بردر نازک) -->
    <item android:top="1dp" android:bottom="1dp" android:left="1dp" android:right="1dp">
        <shape android:shape="rectangle">
            <solid android:color="@color/selectedConfigBackColor" />
            <corners android:radius="32dp" />
            <stroke
                android:width="8dp"
                android:color="@color/black2" /> <!-- بردر نازک -->
        </shape>
    </item>

</layer-list>
